name: DealPulse Weekly — Lundi 7h UTC
on:
  schedule:
    - cron: '0 7 * * 1'
  workflow_dispatch:
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Appel Edge Function deal-pulse-weekly
        run: |
          response=$(curl -s -o /dev/null -w "%{http_code}" \
            -X POST \
            "https://wnphyxusalptwqiimazk.supabase.co/functions/v1/deal-pulse-weekly")
          echo "HTTP status: $response"
